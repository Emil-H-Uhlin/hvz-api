
# General deployment
server.port= ${PORT:8080}
spring.profiles.active= ${SPRING_PROFILE:dev}

# Datasource configuration - overridden for prod in ProdDataSourceConfig bean
spring.datasource.url= jdbc:postgresql://localhost:5432/hvzdb
spring.datasource.username= postgres
spring.datasource.password= 2W1

# Hibernate config
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL95Dialect
spring.jpa.hibernate.ddl-auto=${JPA_DDL:create}

# Logging config
spring.jpa.show-sql= ${SHOW_SQL:true}
logging.level.org.hibernate.stat= debug

# Seeding config
spring.sql.init.platform= postgres
spring.sql.init.mode= always

spring.security.oauth2.resourceserver.jwt.issuer-uri=https://dev-imzcye6l.us.auth0.com/
spring.security.oauth2.resourceserver.jwt.audiences=https://re-hvz.herokuapp.com/
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://dev-imzcye6l.us.auth0.com/.well-known/jwks.json